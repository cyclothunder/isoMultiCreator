#include "commands.h"
#include <QStorageInfo>

Commands::Commands()
{

//    int hddListCount = 0;
//    foreach (const QStorageInfo &deviceList, QStorageInfo::mountedVolumes()){
//           if (deviceList.isValid() && deviceList.isReady()) {
//               if (!deviceList.isReadOnly()) {
//                   hddListCount++;
//               }
//           }
//    }

//        static hardDisk* hdd = new hardDisk[hddListCount+1];
}

Commands::~Commands()
{

}

